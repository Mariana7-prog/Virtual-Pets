namespace Virtual_Pets.Classes.Enums
{
    public enum ItemCategory
    // An enum is a list of named options.
    // It is used when something should only be ONE of a few choices.
    {
        Toy,
        Food,
        Medicine
    }
}
